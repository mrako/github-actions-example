name: Docker

on: [push, pull_request]

jobs:
  eslint:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout
        uses: actions/checkout@v1
      - name: Eslint
        uses: mrako/eslint-action@24cf594
